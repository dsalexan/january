export default function({ store, redirect }) {
  // If the user is not authenticated
  // TODO: removendo apra teste
  // if (store.state.auth.user._id !== 'admin') {
  //   return redirect('/')
  // }
}
